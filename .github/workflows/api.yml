# =============================================================================
# GITHUB ACTIONS - API CI/CD
# =============================================================================
# Ce workflow:
# 1. Lint et test l'API Python à chaque push/PR
# 2. Build l'image Docker
# 3. (Optionnel) Push vers un registry et deploy
#
# Documentation: https://docs.github.com/en/actions
# =============================================================================

name: API CI

on:
  push:
    branches:
      - main
      - dev
    paths:
      # Ne déclenche que si des fichiers de l'API changent
      - 'app/back/**'
      - 'infra/Dockerfile.api'
      - '.github/workflows/api.yml'
  pull_request:
    branches:
      - main
      - dev
    paths:
      - 'app/back/**'
      - 'infra/Dockerfile.api'
      - '.github/workflows/api.yml'

env:
  PYTHON_VERSION: '3.11'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/api

jobs:
  # ---------------------------------------------------------------------------
  # Job 1: Lint & Test
  # ---------------------------------------------------------------------------
  lint-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app/back

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: app/back/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Outils de dev (lint, test)
          pip install ruff pytest pytest-cov httpx

      - name: Lint with Ruff
        run: |
          # Lint le code Python
          ruff check . --output-format=github
        continue-on-error: true  # Ne bloque pas le build pour l'instant

      - name: Check imports
        run: |
          # Vérifie que les imports fonctionnent
          python -c "from app import app; print('✅ App imports OK')"

      # TODO: Ajouter des tests unitaires
      # - name: Run tests
      #   run: |
      #     pytest --cov=. --cov-report=xml
      #   env:
      #     DATABASE_URL: sqlite:///./test.db
      #     JWT_SECRET: test-secret
      #     DEBUG: true

  # ---------------------------------------------------------------------------
  # Job 2: Build Docker Image
  # ---------------------------------------------------------------------------
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: lint-test  # Attend que lint-test soit OK
    permissions:
      contents: read
      packages: write  # Pour push vers GitHub Container Registry

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./app/back
          file: ./infra/Dockerfile.api
          target: production
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        if: github.event_name != 'pull_request'
        run: echo "Image pushed with digest ${{ steps.build.outputs.digest }}"

  # ---------------------------------------------------------------------------
  # Job 3: Deploy (optionnel, à configurer selon ton hébergeur)
  # ---------------------------------------------------------------------------
  # deploy:
  #   name: Deploy to Production
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   
  #   steps:
  #     - name: Deploy to Railway/Render/Fly
  #       run: |
  #         # Exemple pour Railway:
  #         # curl -X POST ${{ secrets.RAILWAY_WEBHOOK_URL }}
  #         #
  #         # Exemple pour Render:
  #         # curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
  #         #
  #         # Exemple pour Fly.io:
  #         # flyctl deploy --remote-only
  #         echo "Deploy step - configure according to your hosting provider"

